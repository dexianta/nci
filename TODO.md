- once finish the branch page, start envs page, basically the same as branch page, with just repos and envs form
  - can save it as `(repo, key, value)`

- save global setting as well

- global loop to fetch existing branch
  - execute script
  - update job page on branch page (every 2 sec)
  
